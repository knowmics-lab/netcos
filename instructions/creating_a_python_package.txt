1) create a requirements.txt per vedere tutti i requirements usati

# Install pipreqs
pip install pipreqs

# Run it on your project
cd /path/to/drug_repurposing
pipreqs src/ --force

# move the created requirements.txt to the project root folder

#create a setup.py in the root folder:

from setuptools import setup, find_packages

def read_requirements():
    """Read requirements from requirements.txt"""
    with open('requirements.txt', 'r') as f:
        return [line.strip() for line in f if line.strip() and not line.startswith('#')]

setup(
    name="drug_repurposing",
    version="0.1.0",
    author="L-F-S",
    description="Network-Enhanced Connectivity Score for drug repurposing",
    packages=find_packages(where="src"),
    package_dir={"": "src"},
    python_requires=">=3.13", # CHECK VERSION
    install_requires=read_requirements(),
)

#create an empty __init__.py file inside /src/ (or in any folder where there are modules u want to import)

# Create a virtual environment
# you will develop your package into this virtual environment
conda create -n drug_repurposing python=3.9
conda activate drug_repurposing

#install packages of your project
conda install pip 
pip install -r requirements.txt

# install your pakcage in developement mode
pip install -e .

# configure Spyder to use your new environment.

# This will install the necessary component for Spyder to communicate with your environment.
pip install spyder-kernels==3.0.*

Open spyder 
In Spyder, go to: Tools → Preferences
In the left panel, click on Python interpreter
Select the option: "Use the following Python interpreter:"
Click the "Browse" button or paste this path:
    C:\Users\los4\miniconda3\envs\drug_repurposing\python.exe

# test if it works:
from conf import DISEASE
print(DISEASE)
from connectivity_score import bin_chen_connectivity
print("Import successful!")