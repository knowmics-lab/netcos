
temporaneamente in standby:

		27/10/2025.
		TRying to move everything to VScode.
		installed git
		installedd git and gitcopilot on VScode
		Should be accessed inside both (github copilot AND git) wit L-F-S
		commit with GUI from vs code by clicking version control on the left hand side > commit & push
		NOn ha funzionato..?


		Ideally, check that the data and log folders are OUT of github, and 
		PULL into the server, (on a separate folder, or make sure that when you pull, it doesnt overrwite the data...)

		31/10/2025. 
		temporarily standby. spyder is just easier.

31/10/2025
Using version control with git and spyder.
git is installed.
spyder open project > right click on file (or folder) in repo file explorer > commit , git GUI opens up: follow that
in git:
cd to the directory
git add filename
git commit -m 'message'
git push

gitignore: has files to be ignored.

LOCAL VERSIOIN IS THE MAIN BRANCH. GITHUB CURRENTLY UP-TO-DATE, WITHOUT ANY DATA FOLDER:
tsr/data/
tsr/output/
/MITHrIL/input/
/MITHrIL/output/
/other_outputs/
/connectivity_score/input/
/connectivity_score/output/

Now, i 
git clone'd the repo into the server, as netcos
I keep 'drug repurposing' folder for backup, and rename it into drug repurposing unversioned
and I want to migrate all data from drug repurposing into netcos folder:

cp -r new_tsr_data ../netcos/new_tsr_data


e' lentiiiiissimo a fare la copia dei files maaaaaaaaaaaaaaammalucarmmine
faccio mv magari?
cmq non funziona l ssh, funge male

when pulling to server,
if git pull fa le bizze
git -- checkout filechefalebizze
git pull