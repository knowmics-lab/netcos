MITHrIL algorithm ouput:

perturbation file (quello che ci interessa):
	list of pathways along with their impact factor along with their perturbation factor, PF, accumulator, pvalue, and adjusted p value
output file:
	the final result of the algorithm consists
	of a list of pathways along with their impact factor,
	accumulator and adjusted p-values. Such list is sorted by
	p-value and Acc.
does it have a propagation value as well? for every gene? is it the perturbation factor PF? Yes, it is.

Domande a Salvatore Alaimo sul paper:
PF formula, is it recursive?
Si recursive, ma c'è un refuso nel paper, il secondo PF(n,Pi) dovrebbe essere PF(u, Pi), per cui è moltiplicato tuttp per il PF del gene upstream, che quindi calcola upstream su tutto. 
Ma questo vuol dire che tutta la rete di mithril è direzionata? 
Si, tutta è direzionata, ma non è a ciclica.
Ma allora se è direzionata ma non a ciclica, come trovo il punto di inizio?? Eh!  lo esprimi in forma matriciale e allora non serve fare le propagazioni.

IF formula:meaning? 
E' una metrica con valore storico, non interessante per me.
second par normalized by mean foldchange*number of differentially expressed genes? Quindi questo numero è minore di tutti i geni che hanno fold change? DE è dato da threshold?.

Accumulator: A bit more sofisticated than a sum: but why remove fold change ? meaning?
remove fold change perche vuole vedere solo l'effetto della perturbazione. xke tipo nell enrichment (che poi inzialmente era nato per fare l'enrichment l'accumulatore) vuole vedere solo la propagazione (che cmq in realtà tiene conto anche del valore iniziale).
In pratica, l'accumulatore da' dei  segni ai nodi, per calcolare la grandezza della inibizione/attivazione della pathway. però in questa cosa i miRNA sono visti come meno, mentre  i geni come piu, e basta. i geni sn sempre piu (attenzione, piu' come proprieta' nei nodi, negli edges no, e i segni dei nodi NON sn presi in considerazioni quando calcoli PF, ma solo con Acc), e la formula usata è un po' diversa da qll sul paper (Acc gene - Acc miRNA), ma è una generalizzaione dove sommi tutto, moltiplicato per una lista di pesi, dove i pesi per i geni sono +1 e per i mirna sono -1, quindi piu o meno è uguale

other paper to check about metapahtways: detectid disease specific pathway substructures alaimo 2017



##################################
#run mithril 3 for single file (Disease data):
##################################

cd drug_repurposing #giusto per avere le path fatte cosi, ma mith3 lo puoi chiamare da ovunque 

java -jar /home/signorini/mithril3/app-3.0.0-SNAPSHOT.jar mithril -verbose -reactome -p -customize-pathway-matrix -seed 1234 -inversion-factory fast-cpu -multiplication-factory fast-cpu -t 30 -organism hsa_v2023_03 -i MITHrIL/input/disease_signature/ipf_signature_gene_id.mi -o MITHrIL/output/disease_signature/ipf_mith3.output.txt -p MITHrIL/output/disease_signature/ipf_mith3.perturbations.txt

#mithril runs parameter runs a single instance of mithril3 (since input is not a batch)
# see java -jar /home/signorini/mithril3/app-3.0.0-SNAPSHOT.jar mithril --help for parameter meaning (ATTENZIONE! sono leggermente diversi dai paramteri di mithril-batch)

#the command above is run (for disease) by step2_disease_run_MITHrIL.py, or inside NetCos_pipeline step Disease - Run MIThRIL

# WARNING: to convert als_NYGC_mith3.perturbations.txt in a format usable from connectivity score calculation scripts
(also takes into account MITHrIL bug in header: There are 8 columns, but 7 column names: last column is adjusted p value, but the column name is missing


python modules/step3_mith_out_to_cs_in_disease.py 
	output:
		CS_IN_DISEASE+'/'DISEASE+'_mith3_signature.csv' '\t' separated file. dovrei chiamarli  .tsv in effetti

--> back to calculate connectivity.
All these steps are found inside the pipeline notebook

###############################
# run mithril 3 for a batch of files (Drug data)       #
###############################

# 1. extract FC data for all genes, for all drugs at 6h, 24h and metanalysis (6h_24h) timestep from Carmelo's metnalayisis gene files running:


cd drug_repurposing/modules
python 4_map_metanalysis_wrapper.py

# check LOG for pseudocode on  map_metanalysis_gene_wise_to_drug_wise
# it actually launches 31 Rscrpits of map_metanalysis_gene_wise_to_drug_wise, each of them iterating over 100 drugs at a time 


# This will save 3222*3 drug_wise files in MITHRIL input in MITHRIL/input/drug_signature/DRUGNAME_timepointh.mi, with two columns, tab separated, first with gene name, second with FC (no header, no index). These are viable input for mithril2
# Additionally, it will save 3222 (not*3) drug_wise .Rds files in tsr/output/drug_signature/LINCS/metanalysis_drug_wise, with same columns as metanalysis Rds files, but with genes as rows, and drugs as files. # these files will be used to compare to mithril output which is drug based, and also to run connectivity score calculations with new algorithm. 
# ADDITIONAL STEP to filter data inside metanalysis_drug_wise, because they are unfortunately still DEG data with non unique gnee filters, run the dedicated section inside preproc_utils.py, create a folder called metanalysis_drug_wise_filtered, and then zip the folder metanalysis_drug_wise.

# Create MITHRIL3 input, chiamato LINCS_metanalysis.mi by merging all the drug-wise files in MITHRIL/input/drug_signature/ files into one:
# formato input mithril 3:

exp1    exp2    exp3
g1  v1  v2  v3
g2  v1  v2  v3

#run:

step5_mith2_to_mith3_durg_input.py

#this will then, translate the gene names to ids (for mithril, not necessary but advised), IF POSSIBLE. (if not, gene name will be kept)
#output file: LINCS_metanalysis.mi.
# this output file's first column are gene IDs, but the first row does not have a label for them, so the label starts from the first drug. Tab separated. Which means there are x columns and x-1 columns int the first rows


# Lancia MIThRIL3 per tutte le drug contemporanemanete usando mithril-batch e la matrice LINCS_metanalysis.mi

java -jar /home/signorini/mithril3/app-3.0.0-SNAPSHOT.jar mithril-batch -verbose -reactome -p -customize-pathway-matrix -seed 1234 -inversion-factory fast-cpu -multiplication-factory fast-cpu -t 30 -i MITHRIL/input/drug_signature/LINCS_metanalysis_old.mi -o MITHRIL/output/drug_signature -p

# 11/04/2025 run with latest hsa database (same just different output)
java -jar /home/signorini/mithril3/app-3.0.0-SNAPSHOT.jar mithril-batch -reactome -p -customize-pathway-matrix -seed 1234 -inversion-factory fast-cpu -multiplication-factory fast-cpu -t 30 -i /home/signorini/drug_repurposing/MITHrIL/input/drug_signature/LINCS_metanalysis.mi  -o /home/signorini/drug_repurposing/MITHrIL/output/drug_signature_2025/ -p -verbose


-organism hsa
parametri:
-reactome: utilizza anche il network di reactome (piu grande e piu accuranto, mithril2 non ce l aveva)
-p è solo un flag ora per  diregli di fare il perturbation file
-customize-pathway-matrix : calcola alcone cose come mithril 2. va messo
-inversion-factory fast-cpu -multiplication-factory fast-cpu usa la libreria fast-cpu per fare certi calcoli
-t numero thread
-o DIRECTOY di output 

#MITHRIL3 output: 3222*3*2 drug files saved in MITHRIL/output/drug_signatures/drugname_timestep.output.txt and .perturbations.txt

# so this  prints the PATHWAYS in which a given gene appears!
# every line is A GENE IN A PATHWAY, but its value of acc,pval, adjusted pval and pert is the same in every pathway.

#######################################################################
## ATTENZIONE COMPORTAMENTO INSOLITO NEL FILE NAMING DI MITHRIL OUTPUT!
######################################################################
Non propriuo un bug ma un comportamento voluto in cui quando scrive il file, cambia tutti i caratteri che non sono alfanumerici, '-'' o '_' in '-', perché sono caratteri che possono creare problemi di sicurezza. 
# TO FIX IT, and resume normal filenames:

python stepX_check_name_diff_mith_in_out.py

######################################################
# ATTENZIONE BUG NEL COLUMN NAME DI MITHRIL OUTPUT
#######################################################

Manca il nome dell'ultima colonna, che è adj_pValue. (la nuova versione non richiede il nome della colonna per salvare un input per il CS col nome della colonnadi adjusted pvalue)


# Trasforma l'output di mithril in input per connectivity score. L'input cs per la durg è un file binario per salvare tempo e spazio
	python step7_drug_mith_out_to_cs_in.py # ci mette circa 20 minuti con 30 cores. dovrebbe essere piu veloce ora.

	#prende ogni output di mithril.perturbation, che è un file con 250k o 270 nn ricordo rows, una per  patwhay per gene, e lo traforma in un metanalysis di tsr pronto per essere preso in input dal connectivity score, concatenando i 3 timestep perturbations, e levando le pathway e tenendo una row per gene (geni uguali in pathway diverse hanno la stessa perturbation value ,quindi il mith3 output è i nrealta pieno di doppioni), e agginugendo una t-value like statistic (chè non è proprio uguale al tvalue xke il tvalue ha anche il segno

	output:
		se save_csv=True (sconsigliato, rallenta, ma serve per vedere il singolo file)
		LINCS/metanalysis_mith3_drug_wise/DRUGNAME_metanalysis.csv #tab separated file, 
		CS_IN_DRUG/drugname_metanalysis.pkl # binary file




