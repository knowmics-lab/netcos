# -*- coding: utf-8 -*-
"""
This script has moved.

New location:
    src/validations/chembl/call_cs_batch_for_different_confs.py

The grid search is part of the ChEMBL validation protocol, so it now lives
next to the other validation scripts. The grid itself was extracted into
src/validations/chembl/grid.py and is shared with
hyperparameter_grid_search_bootstrap.py.

Run from src/validations/chembl/:
    python call_cs_batch_for_different_confs.py
"""
import sys

print(__doc__, file=sys.stderr)
sys.exit(2)
