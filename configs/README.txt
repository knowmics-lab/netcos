Config files for various setups of NetCos Runs
substitute files inside netcos/src with files inside every config folder to execute given setup
and rename the specitifc conf file to conf.py

Ideally, every run should only change its conf.py,
but for historical reasons this is not true


-05 06 2026 Bin Chen 2017 replication + IC50 validation	
	conf.py + all other scripts inside src